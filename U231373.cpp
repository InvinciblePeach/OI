#include <bits/stdc++.h>

using namespace std;

int n, x, p, q;
int nums[100005];

int main() {
    cin >> n >> x >> p >> q;
    for (int i = 1; i <= n; i ++) {
        cin >> nums[i];
    }
    return 0;
}